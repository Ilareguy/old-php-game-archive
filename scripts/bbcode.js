/*   Cette page contient les fonctions en rapports avec    les messages comportant du BBcode ou des smileys, tel   que les messages dans le forum ou les messages privés.      Par Anthony Ilareguy.   webmasters@berceaudeguerres.com*/function insert_bbcode(balise1, balise2){   if(navigator.appCodeName == "Mozilla"){      var textarea = window.document.formulaire_message.message_textarea;      textarea.focus();      var selection_debut = textarea.selectionStart;      var selection_fin = textarea.selectionEnd;      var selection = textarea.value.substring(selection_debut, selection_fin);      var nouvelle_selection = balise1 + selection + balise2;      textarea.value = textarea.value.substr(0, selection_debut) + nouvelle_selection + textarea.value.substr(selection_fin);   }   else{      alert("Vous devez utiliser Mozilla Firefox pour utiliser correctement le BBcode.\nVous pouvez toujours entrer les smileys et le BBcode manuellement.");   }}function insert_smiley(smiley){   var textarea = window.document.formulaire_message.message_textarea;   textarea.focus();   var selection_debut = textarea.selectionStart;   var selection_fin = textarea.selectionEnd;   var selection = textarea.value.substring(selection_debut, selection_fin);   var nouvelle_selection = selection + smiley;   textarea.value = textarea.value.substr(0, selection_debut) + nouvelle_selection + textarea.value.substr(selection_fin);}